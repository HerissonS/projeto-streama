version: '3'
services:
  streama:
    image: anapsix/alpine-java:8
    restart: unless-stopped
    volumes:
    - /mnt/efs/ctfoco/streama-1.10.4.jar:/app/app.jar
    - /mnt/efs/ctfoco/application.yml:/app/application.yml
    - /mnt/efs/ctfoco/volumes/streama/uploads:/app/uploads
    - /mnt/efs/ctfoco/volumes/streama/local-files:/app/local-files
    working_dir: /app
    command: java -jar app.jar
    network_mode: "host"
    environment:
    - VIRTUAL_HOST=localhost
    #ports:
    #- "80:80"
